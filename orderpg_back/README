python3 -m venv venv para generar un ambiente
para usar el ambiente creado
source venv/bin/activate
instalar
pip install flask flask-cors requests
para instalar el archivo requirements.txt
pip install -r requirements.txt